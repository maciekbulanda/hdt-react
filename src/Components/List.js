import React, {useState, useEffect} from 'react'
import axios from 'axios'

const List = () => {
    let [readings, setReadings] = useState([])

    useEffect(() => {
        axios.get()
    }, [])

    return (
        <div>
            
        </div>
    )
}

export default List